<% if params[:page_id].blank? %>
	<%= form.input :page_id, collection: Page.all.order("name DESC") %>
<% else %>
	<%= form.input :page_id, selected: params[:page_id], collection: @pages %>
<% end %>
<%= form.input :title %>
<%= form.input :body, as: :textile_editor %>
<%= form.input :image %>